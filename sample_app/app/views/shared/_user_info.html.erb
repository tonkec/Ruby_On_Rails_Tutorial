<%= link_to gravatar_for(current_user), current_user %>
<h1 class="user-name">
  <%= current_user.name %>
  <span class="user-name-span">(<%= pluralize(current_user.microposts.count, "micropost") %>)</span>
</h1>
<span class="link-to-profile"><%= link_to "view my profile", current_user %></span>
